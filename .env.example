# Beta matrix source (local path or S3 URI)
BETA_MATRIX_PATH=data/raw/beta_matrix.csv

# Clinical metadata spreadsheet (local path or S3 URI)
CLINICAL_METADATA_PATH=data/raw/clinical_metadata.xlsx

# TypeDB connection
TYPEDB_ADDRESS=localhost:1729
TYPEDB_DATABASE=methylation_graph

# AWS (optional — uses default credential chain if not set)
# AWS_PROFILE=default
# AWS_REGION=eu-west-2

# Development mode
EPIGRAPH_ENV=dev
